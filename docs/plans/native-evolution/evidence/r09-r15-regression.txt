R09–R15 implementation acceptance mapping (2026-09-20)

R09-A01/A02/A03: tests/reconciliation.rs and tests/proposals.rs: complementary,
multivalued, contextual and conflicting records preserve active originals;
independent sources SUPPORT, same source-event replay does not add evidence.
R09-A04/A05/A06: tests/proposals.rs + tests/evolution_contract.rs: multi-target
CAS, stale approvals, idempotent application, semantic links with inverse and
replacement, dependency-aware compensation, MCP refusal of approval and pending
body/link semantics. Approval audit and fact history share the R07 journal.

R10-A01..A05: tests/maintenance.rs: fixed clock per_day initialization, same-day
idempotence, legacy per_run, backwards clock, pinned expiry/supersession policy,
status/verification preservation, dry-run bytes/history, saturating/non-negative
decay. Integration path uses the R07 revision planner.

R11-A01..A06: tests/sleep.rs + tests/proposals.rs + tests/evolution_contract.rs:
o models/network, source/checkpoint metadata, deterministic proposal IDs, no
cursor advance after invalid/partial output, bounds and cursor validation,
credential-free metadata reports, no approval or shell/cron execution.
Normal paging returns partial + next_cursor. Oversized structural/byte inputs
fail explicitly rather than promising recovery through an unbounded scan.

R12-A01..A05: tests/views.rs: exact source references/revisions, stale source,
missing refs with explicit partial, zero evidence contribution, no default search/
context injection, unchanged core, modified managed file conflict, unmanage,
offline template index without claiming semantic synthesis.

R13-A01..A06: tests/snapshots.rs + tests/native_upgrade_smoke.py: canonical,
history, provenance, checkpoints and proposal bytes survive restore; caches
rebuild. Unsafe/duplicate/corrupt paths, symlinks/hardlinks, size limits and pending
journals are refused. Config/transcripts excluded. Restore keeps identity, fork
remaps and invalidates copied approvals; duplicate active identities are detected.
Old per-memory .md.lock files are excluded at creation, rejected in manifests.

R14-A01/A02/A03/A04/A06: native_perf.py, native_eval_matrix.py, existing retrieval
thresholds, HTTP repair mocks, R07 recovery/concurrency and native search suites.
Requested/effective lanes are explicit. Reranker absent is a verified fallback,
not a successful real rerank. No speculative performance optimization was made.
R14-A05: PASS for the documented bounded synthetic comparison. Actual fixed
Luna max Codex CLI consumers used fresh isolated sessions, one identical task,
and equal input/output prompt budgets for no memory / baseline 1.0.0 retrieval /
candidate native prep+checkpoint context. Final facts correct: 0/2, 2/2, 2/2;
exact next-action recovery: false, false, true; unsupported answers: 0 in all.
The producer checkpoint is a fixture, not a separately measured producer model.
This is one task and not statistically generalizable or live-host acceptance.
An earlier native-prep run exposed lost checkpoint next_action due to the generic
summary cap; that evidence is retained and the corrected path has a unit regression.

R15-A01..A05: explicit plan statuses and bilingual compatibility docs; package
allowlist/offline build; fixed baseline 1.0.0 temporary-store upgrade/restore;
existing four-host protocol fixtures and 12 directed handoff combinations.
New Linux workflow is configured but not executed remotely in this local task.
Real ONNX assets, real host/model sessions, installation, production migration
and new publication are NOT_RUN. The local installed path reported 0.8.0; the
upgrade baseline instead was built from fixed d05f02853f42771ae9ca04d78b79739d01f1a76c.
No actual memory contents or host settings were changed.

Commands and exit codes: evidence/final/results.json and per-gate logs.
Performance measurements: evidence/r14-perf-final.json (release, three samples).
R14 model evidence: evidence/r14-agent-eval.json, generated by the opt-in
native_agent_eval.py harness (not invoked by the offline acceptance runner).
