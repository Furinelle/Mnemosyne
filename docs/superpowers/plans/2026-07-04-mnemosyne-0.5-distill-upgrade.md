# Mnemosyne 0.5.0 蒸馏升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 蒸馏（distill）路径的三项升级：Stop hook 增量化（只处理新增轮次）、Finding 溯源字段 + LLM 引擎 typed observations 与可选会话摘要、`write` 路径统一接入 dedup 判定。

**Architecture:** 增量状态存 store 根目录 `.distill_state.json`（与 0.4.0 的 `.session_injected.json` 同模式）；`Finding` 增加可选 `evidence` 字段贯穿 heuristic/llm/host 三引擎并持久化到 frontmatter `extra`；`session_summary` 成为新记忆类型（LLM 引擎在 `[distill].session_summary = true` 时输出）；`cmd_write` 复用 `classify_against_store`，`--force` 只跳过交互确认、不再跳过 dedup，新增 `--allow-duplicate` 逃生阀。

**Tech Stack:** Python 3.11+，stdlib + portalocker，pytest（经 uv 运行）。

## Global Constraints

- Python 3.11+；核心路径只允许 stdlib + `portalocker>=2.8`，不得新增硬依赖；LLM 调用保持 opt-in（无 API key 时静默降级）。
- 测试命令固定为：`uv run --with pytest --with portalocker -- python -m pytest -q`，当前基线 140 passed，每个任务结束时必须全绿。
- 记忆文件格式向后兼容：`evidence` 走 `Memory.extra`（parse/serialize 已天然支持未知 frontmatter 键），旧文件无需迁移。
- hooks 永不阻塞 Claude：`hook_safe()` 包裹不变；增量状态读写失败必须静默降级为全量行为。
- 引号风格随文件：`mnemosyne/hooks/*.py` 单引号，其余核心模块双引号。
- 提交信息遵循仓库风格：`fix(scope):` / `feat(scope):` / `chore:`。

---

### Task 1: distill 增量化（Stop hook 只处理新增轮次）

**背景**：Claude Code 的 Stop hook 在每次回复结束触发，当前 `_maybe_distill` 每次解析并蒸馏整个 transcript，长会话成本线性叠加（幂等只靠 dedup 兜底）。按 transcript 路径记录已处理轮次数，每次只蒸馏 `turns[done:]`。

**Files:**
- Modify: `mnemosyne/distill/__init__.py`（新增状态函数）
- Modify: `mnemosyne/hooks/stop.py:43-65`（`_maybe_distill`）
- Test: `tests/test_hooks.py`

**Interfaces:**
- Produces: `distill.load_processed_turns(transcript_key: str) -> int`、`distill.record_processed_turns(transcript_key: str, count: int) -> None`
- 状态文件：`<store.root>/.distill_state.json`，结构 `{"transcripts": {"<path>": {"ts": "<iso>", "turns": N}}}`，7 天 TTL 清理
- 契约：`done > len(turns)` 视为 transcript 已轮换，从 0 重新处理；无论是否有 finding，处理后都记录 `len(turns)`

- [ ] **Step 1: 写失败测试**

`tests/test_hooks.py` 末尾追加：

```python
def test_processed_turns_roundtrip(tmp_path, monkeypatch):
    from mnemosyne import distill

    monkeypatch.setenv("MNEMOSYNE_HOME", str(tmp_path / "home"))
    (tmp_path / "home").mkdir()
    monkeypatch.chdir(tmp_path)  # no project store -> state lives in global root

    assert distill.load_processed_turns("/a/t.jsonl") == 0
    distill.record_processed_turns("/a/t.jsonl", 4)
    assert distill.load_processed_turns("/a/t.jsonl") == 4
    distill.record_processed_turns("/a/t.jsonl", 7)
    assert distill.load_processed_turns("/a/t.jsonl") == 7
    assert distill.load_processed_turns("/other.jsonl") == 0


def test_stop_hook_processes_only_new_turns(tmp_path, monkeypatch, capsys):
    monkeypatch.setenv("MNEMOSYNE_HOME", str(tmp_path / "global"))
    monkeypatch.chdir(tmp_path)
    store = project_store()
    ensure_store(store)
    store.config_path.write_text(
        store.config_path.read_text(encoding="utf-8").replace(
            "enabled = false\nengine", "enabled = true\nengine"
        ),
        encoding="utf-8",
    )
    transcript = tmp_path / "t.jsonl"
    transcript.write_text(
        '{"type":"user","message":{"role":"user","content":"不要用 print 调试，改用 logging"}}',
        encoding="utf-8",
    )
    event = json.dumps({"transcript_path": str(transcript), "stop_hook_active": False})

    monkeypatch.setattr("sys.stdin", io.StringIO(event))
    stop.main()
    capsys.readouterr()
    first = load_memories(store)
    assert len(first) == 1

    # Delete the saved memory: if the hook re-processed old turns it would be
    # recreated; incremental processing must leave the store empty.
    first[0][0].unlink()
    monkeypatch.setattr("sys.stdin", io.StringIO(event))
    stop.main()
    capsys.readouterr()
    assert load_memories(store) == []

    # A genuinely new turn is still picked up.
    with transcript.open("a", encoding="utf-8") as handle:
        handle.write(
            '\n{"type":"user","message":{"role":"user","content":"别用 pip，改用 uv 安装依赖"}}'
        )
    monkeypatch.setattr("sys.stdin", io.StringIO(event))
    stop.main()
    capsys.readouterr()
    assert len(load_memories(store)) == 1
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q -k "processed_turns or only_new_turns"`
Expected: FAIL（`load_processed_turns` 不存在；第二次 stop.main() 重建了被删除的记忆）

- [ ] **Step 3: 实现状态函数**

`mnemosyne/distill/__init__.py`：头部 import 增加 `import os`、`from datetime import datetime, timedelta`、`from mnemosyne.store import global_store`（并入现有 store import 行）。`Turn` 定义之后追加：

```python
DISTILL_STATE_FILENAME = ".distill_state.json"
DISTILL_STATE_TTL_HOURS = 168  # transcripts can span days; prune after a week


def _distill_state_path() -> Path:
    from mnemosyne.store import find_project_store

    project = find_project_store()
    root = project.root if project is not None else global_store().root
    return root / DISTILL_STATE_FILENAME


def load_processed_turns(transcript_key: str) -> int:
    if not transcript_key:
        return 0
    try:
        data = json.loads(_distill_state_path().read_text(encoding="utf-8"))
    except (OSError, ValueError):
        return 0
    entry = data.get("transcripts", {}).get(transcript_key, {})
    try:
        return max(0, int(entry.get("turns", 0)))
    except (TypeError, ValueError):
        return 0


def record_processed_turns(transcript_key: str, count: int) -> None:
    """Remember how many turns of a transcript have been distilled.

    The Stop hook fires after every assistant reply; without this the whole
    transcript is re-parsed and re-extracted each turn. Failures degrade
    silently to full re-processing (dedup still guards correctness).
    """
    if not transcript_key:
        return
    path = _distill_state_path()
    try:
        data = json.loads(path.read_text(encoding="utf-8"))
    except (OSError, ValueError):
        data = {}
    if not isinstance(data, dict):
        data = {}
    transcripts = data.setdefault("transcripts", {})
    now = datetime.now()
    cutoff = now - timedelta(hours=DISTILL_STATE_TTL_HOURS)
    for key in list(transcripts):
        try:
            stamp = datetime.fromisoformat(str(transcripts[key].get("ts", "")))
        except (AttributeError, ValueError):
            stamp = None
        if stamp is None or stamp < cutoff:
            del transcripts[key]
    transcripts[transcript_key] = {"ts": now.isoformat(), "turns": max(0, int(count))}
    try:
        path.parent.mkdir(parents=True, exist_ok=True)
        tmp = path.with_suffix(".tmp")
        tmp.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")
        os.replace(tmp, path)
    except OSError:
        pass
```

- [ ] **Step 4: 改造 `_maybe_distill`**

`mnemosyne/hooks/stop.py` 的 `_maybe_distill` 中，`from mnemosyne.distill import ...` 行改为引入增量函数，并只蒸馏新增轮次：

```python
    from mnemosyne.distill import (
        distill_text,
        load_processed_turns,
        parse_claude_transcript,
        record_processed_turns,
        turns_to_text,
    )

    turns = parse_claude_transcript(transcript_path)
    if not turns:
        return ""
    key = str(transcript_path)
    done = load_processed_turns(key)
    if done > len(turns):
        done = 0  # transcript rotated/replaced; start over
    new_turns = turns[done:]
    if not new_turns:
        return ""
    actions = distill_text(turns_to_text(new_turns), source="claude-code", commit=True)
    record_processed_turns(key, len(turns))
```

（`saved = ...` 起的后续行保持不变。）

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 142 passed

- [ ] **Step 6: 提交**

```bash
git add mnemosyne/distill/__init__.py mnemosyne/hooks/stop.py tests/test_hooks.py
git commit -m "feat(distill): incremental Stop-hook distillation via .distill_state.json"
```

---

### Task 2: Finding.evidence 溯源字段（heuristic/host 引擎 + 持久化）

**背景**：蒸馏写入的记忆无法回溯来源。给 `Finding` 加可选 `evidence` 字段，host 引擎（`**新发现:**` 块）支持 `- evidence:` 行，`write_finding` 把它持久化到 frontmatter（走 `Memory.extra`，格式天然兼容）。heuristic 引擎的 content 本身就是逐字 turn 文本（自证），evidence 留空。

**Files:**
- Modify: `mnemosyne/codex.py`（`Finding` 数据类、`parse_findings`、`write_finding`）
- Test: `tests/test_codex.py`

**Interfaces:**
- Produces: `Finding(type, importance, title, tags, content, evidence="")` —— 新字段有默认值，既有 5 参构造全部兼容
- `write_finding` 在 `finding.evidence` 非空时写 `memory.extra["evidence"] = finding.evidence[:200]`

- [ ] **Step 1: 写失败测试**

`tests/test_codex.py` 末尾追加（沿用该文件既有风格；若为 unittest 类则放入新类，import 处补 `isolated_workspace`）：

```python
class EvidenceTests(unittest.TestCase):
    def test_parse_findings_reads_evidence_field(self) -> None:
        from mnemosyne.codex import parse_findings

        text = (
            "**新发现:**\n"
            "- type: pitfall\n"
            "- importance: 70\n"
            "- title: cache bug\n"
            "- tags: cache\n"
            "- evidence: turn 3, assistant\n"
            "- content: |\n"
            "    stale cache caused the failure\n"
        )

        findings = parse_findings(text)

        self.assertEqual(1, len(findings))
        self.assertEqual("turn 3, assistant", findings[0].evidence)

    def test_write_finding_persists_evidence(self) -> None:
        from mnemosyne.codex import Finding, write_finding
        from mnemosyne.store import find_memory, stores_for_scope

        with isolated_workspace():
            from mnemosyne.cli import main

            self.assertEqual(0, main(["init"]))
            finding = Finding(
                type="pitfall", importance=70, title="cache bug",
                tags=["cache"], content="stale cache caused the failure",
                evidence="turn 3, assistant",
            )

            memory_id = write_finding(finding, "test")

            found = find_memory(memory_id, stores_for_scope("all"))
            self.assertIsNotNone(found)
            self.assertEqual("turn 3, assistant", found[2].extra.get("evidence"))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_codex.py -q -k Evidence`
Expected: FAIL（`Finding` 无 evidence 参数；parse_findings 丢弃 evidence 行）

- [ ] **Step 3: 实现**

`mnemosyne/codex.py`：

1. `Finding` 数据类加字段：

```python
@dataclass
class Finding:
    type: str
    importance: int
    title: str
    tags: list[str]
    content: str
    evidence: str = ""
```

2. `parse_findings` 的字段行处理：`elif current is not None and key in ('importance', 'title', 'tags'):` 改为 `elif current is not None and key in ('importance', 'title', 'tags', 'evidence'):`；`flush()` 中 `findings.append(...)` 前加 `evidence = (current.get('evidence') or '').strip()`，append 行改为 `findings.append(Finding(type_value, importance, title[:80], tags, content, evidence))`。

3. `write_finding` 中 `path = working_path(store, memory)` 之前加：

```python
    if finding.evidence:
        memory.extra['evidence'] = finding.evidence[:200]
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 144 passed

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/codex.py tests/test_codex.py
git commit -m "feat(codex): Finding.evidence provenance field, parsed from findings blocks and persisted"
```

---

### Task 3: LLM 引擎升级 —— evidence 输出 + 可选 session_summary

**背景**：LLM 引擎已能输出 typed findings（pitfall/arch_decision/codebase/preference/handoff），补两块：要求模型给出 `evidence`（对话原文短引用），以及 `[distill].session_summary = true` 时额外输出一条 `session_summary` 类型的会话摘要。`session_summary` 成为合法记忆类型。

**Files:**
- Modify: `mnemosyne/distill/llm.py`（prompt 构建、JSON 解析）
- Modify: `mnemosyne/codex.py`（`ALLOWED_TYPES` 加 `session_summary`）
- Modify: `mnemosyne/store.py`（`DEFAULT_CONFIG` / `DEFAULT_CONFIG_TOML`：`memory.types` 加 `session_summary`；`[distill]` 加 `session_summary = false`）
- Modify: `README.md`（记忆类型表、`[distill]` 配置说明）
- Test: `tests/test_distill.py`

**Interfaces:**
- Produces: `llm._build_prompt(conversation: str, include_session_summary: bool) -> str`；`_parse_llm_json` 解析可选 `evidence` 键；`LLMExtractor.__init__` 读取 `config["distill"]["session_summary"]`
- 契约：`session_summary` 进入 `ALLOWED_TYPES`（host/codex-ingest 同步接受该类型）

- [ ] **Step 1: 写失败测试**

`tests/test_distill.py` 末尾追加：

```python
def test_parse_llm_json_reads_evidence_and_session_summary():
    payload = (
        '[{"type": "session_summary", "importance": 55, "title": "今日会话摘要",'
        ' "tags": ["session"], "content": "重构了认证模块", "evidence": "user: 重构认证"}]'
    )

    findings = _parse_llm_json(payload)

    assert len(findings) == 1
    assert findings[0].type == "session_summary"
    assert findings[0].evidence == "user: 重构认证"


def test_build_prompt_toggles_session_summary():
    from mnemosyne.distill.llm import _build_prompt

    with_summary = _build_prompt("[user] hi", include_session_summary=True)
    without_summary = _build_prompt("[user] hi", include_session_summary=False)

    # The type list always mentions session_summary; the *instruction* to emit
    # one is what the config toggles.
    assert "evidence" in without_summary
    assert "exactly one" in with_summary
    assert "exactly one" not in without_summary
    assert with_summary.endswith("[user] hi")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_distill.py -q -k "evidence_and_session or toggles"`
Expected: FAIL（`session_summary` 类型被丢弃；`_build_prompt` 不存在）

- [ ] **Step 3: 实现**

`mnemosyne/codex.py`：`ALLOWED_TYPES` 改为：

```python
ALLOWED_TYPES = ('arch_decision', 'pitfall', 'codebase', 'preference', 'handoff', 'session_summary')
```

`mnemosyne/distill/llm.py`：模块级 `_PROMPT` 替换为构建函数，`_parse_llm_json` 解析 evidence，`extract` 接线：

```python
_BASE_PROMPT = (
    "You extract durable memories from a developer/agent conversation. "
    "Return ONLY a JSON array. Each element: "
    '{"type": one of '
    + "|".join(ALLOWED_TYPES)
    + ', "importance": 50-90, "title": <=80 chars, "tags": [..], "content": "...",'
    ' "evidence": short verbatim quote from the conversation supporting this memory}. '
    "Only include genuinely reusable facts (pitfalls, decisions, preferences, codebase, handoff). "
    "Empty array if nothing is worth saving."
)

_SUMMARY_INSTRUCTION = (
    " Additionally, include exactly one element with type \"session_summary\": "
    "2-4 sentences on what was worked on and the outcome, importance 55."
)


def _build_prompt(conversation: str, include_session_summary: bool) -> str:
    prompt = _BASE_PROMPT
    if include_session_summary:
        prompt += _SUMMARY_INSTRUCTION
    return prompt + "\n\nCONVERSATION:\n" + conversation
```

`_parse_llm_json` 中 `tags = ...` 之后：

```python
        evidence = str(item.get("evidence", "")).strip()[:200]
        findings.append(Finding(type_value, importance, title, tags, content, evidence))
```

（替换原 `findings.append(Finding(type_value, importance, title, tags, content))`。）

`LLMExtractor`：

```python
class LLMExtractor:
    def __init__(self, config: dict, max_findings: int = 5) -> None:
        distill_cfg = config.get("distill", {})
        self.llm_cfg = distill_cfg.get("llm", {})
        self.include_session_summary = bool(distill_cfg.get("session_summary", False))
        self.max_findings = max_findings

    def extract(self, turns) -> list[Finding]:
        from mnemosyne.distill import turns_to_text

        api_key = os.environ.get(self.llm_cfg.get("api_key_env", "OPENAI_API_KEY"), "")
        if not api_key:
            import sys

            print("mnemosyne: distill.llm enabled but API key missing; skipping", file=sys.stderr)
            return []
        payload = self._call_api(
            _build_prompt(turns_to_text(turns), self.include_session_summary), api_key
        )
        return _parse_llm_json(payload)[: self.max_findings]
```

（`_call_api` 不变。）

`mnemosyne/store.py`：`DEFAULT_CONFIG["memory"]["types"]` 与 `DEFAULT_CONFIG["distill"]` 加：

```python
    "memory": {
        "types": ["arch_decision", "pitfall", "codebase", "preference", "handoff", "session_summary"],
    },
```

`"distill"` 段 `"engine": "heuristic",` 之后加一行 `"session_summary": False,`。`DEFAULT_CONFIG_TOML` 同步：`types = [...]` 行加 `'session_summary'`；`[distill]` 段 `engine = "heuristic"` 下一行加 `session_summary = false`。

`README.md`：记忆类型表加一行 `| session_summary | LLM 蒸馏生成的会话级摘要（[distill].session_summary = true 时输出）。 |`；`[distill]` 配置示例加 `session_summary = false`，说明列表补一句：「`distill.session_summary` 开启后（需 `engine = "llm"`），每次蒸馏额外产出一条 `session_summary` 会话摘要；LLM findings 会带 `evidence` 原文引用便于溯源。」

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 146 passed（注意既有 `test_parse_llm_json_drops_unknown_type` 用的未知类型不在新 `ALLOWED_TYPES` 内才继续有效；若该用例用了 `session_summary` 作反例则改用 `banana`）

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/distill/llm.py mnemosyne/codex.py mnemosyne/store.py README.md tests/test_distill.py
git commit -m "feat(distill): LLM engine emits evidence quotes and optional session_summary type"
```

---

### Task 4: write 路径统一 dedup（--force 不再跳过判定）

**背景**：`--force`（agent 标准用法）目前完全绕过重复检测，防重靠 CLAUDE.md 约定。改为：`cmd_write` 一律先过 `classify_against_store` —— `duplicate` 则跳过写入并打印既有 ID（`--allow-duplicate` 可强写）；`supersede` 则写入后自动建链并降级旧记忆；交互式 `duplicate_prompt` 行为保留（在自动判定之后，仅 tty 且非 force 时）。

**Files:**
- Modify: `mnemosyne/cli.py`（`build_parser` 的 write 子命令、`cmd_write`）
- Modify: `README.md`（常用命令表 write 行）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Consumes: `distill.classify_against_store(finding, *, dedup_threshold, subject_threshold) -> tuple[str, str | None]`、`distill._apply_supersedes(new_id, old_id)`（模块内既有）
- Produces: `write --allow-duplicate` 新 flag；duplicate 时 stdout 含 `Duplicate of <id>` 且退出码 0

- [ ] **Step 1: 写失败测试**

`tests/test_review_fixes.py` 末尾追加：

```python
class WriteDedupTests(unittest.TestCase):
    def test_force_write_skips_exact_duplicate(self) -> None:
        import io
        from contextlib import redirect_stdout

        with isolated_workspace():
            self.assertEqual(0, main(["init"]))
            args = ["write", "--type", "pitfall", "--importance", "70", "--force",
                    "--title", "JWT note", "--content", "use httpOnly cookie for JWT"]
            self.assertEqual(0, main(args))
            output = io.StringIO()

            with redirect_stdout(output):
                code = main(args)

            self.assertEqual(0, code)
            self.assertIn("Duplicate of", output.getvalue())
            store = project_store()
            self.assertEqual(1, len(load_memories(store)))

    def test_allow_duplicate_writes_anyway(self) -> None:
        with isolated_workspace():
            self.assertEqual(0, main(["init"]))
            args = ["write", "--type", "pitfall", "--importance", "70", "--force",
                    "--title", "JWT note", "--content", "use httpOnly cookie for JWT"]
            self.assertEqual(0, main(args))
            self.assertEqual(0, main(args + ["--allow-duplicate"]))

            self.assertEqual(2, len(load_memories(project_store())))

    def test_same_subject_write_supersedes(self) -> None:
        with isolated_workspace():
            self.assertEqual(0, main(["init"]))
            self.assertEqual(0, main([
                "write", "--type", "arch_decision", "--importance", "60", "--force",
                "--title", "JWT storage decision",
                "--content", "use localStorage for JWT",
            ]))
            store = project_store()
            old_id = load_memories(store)[0][1].id

            self.assertEqual(0, main([
                "write", "--type", "arch_decision", "--importance", "60", "--force",
                "--title", "JWT storage decision",
                "--content", "switch to httpOnly cookie for JWT storage",
            ]))

            memories = {m.id: m for _, m in load_memories(store)}
            self.assertEqual(2, len(memories))
            old = memories[old_id]
            self.assertLess(old.strength, 60, "superseded memory must be demoted")
            self.assertTrue(
                any(link.get("rel") == "superseded_by" for link in old.links),
                "old memory must carry the superseded_by backlink",
            )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py -q -k WriteDedup`
Expected: FAIL（重复写入产生 2 个文件；无 supersede 链）

- [ ] **Step 3: 实现**

`mnemosyne/cli.py`：

1. `build_parser` 中 write 子命令 `--force` 行之后加：

```python
    write_parser.add_argument("--allow-duplicate", action="store_true",
        help="skip duplicate detection and write anyway")
```

2. `cmd_write` 中，`--expires` 警告之后、交互式 `duplicate_prompt` 之前插入（延迟 import 规避 codex↔cli 循环依赖）：

```python
    # Dedup applies to every write path, including --force: agents write with
    # --force by default, which used to bypass duplicate detection entirely
    # and left "search before write" as a convention instead of a mechanism.
    from mnemosyne.codex import Finding
    from mnemosyne.distill import classify_against_store

    distill_cfg = config.get("distill", {})
    verdict, target = classify_against_store(
        Finding(type=args.type, importance=memory.strength, title=title, tags=tags, content=content),
        dedup_threshold=float(distill_cfg.get("dedup_threshold", 0.85)),
        subject_threshold=float(distill_cfg.get("subject_threshold", 0.5)),
    )
    if verdict == "duplicate" and target and not args.allow_duplicate:
        print(f"Duplicate of {target}; skipped (use --allow-duplicate to write anyway).")
        return 0
```

3. `cmd_write` 末尾，`update_search_index(store, path, memory)` 之后、`print(f"Wrote {memory.id}")` 之前加：

```python
    if verdict == "supersede" and target:
        from mnemosyne.distill import _apply_supersedes

        _apply_supersedes(memory.id, target)
        print(f"Supersedes {target} (linked, old memory demoted).")
```

`README.md` 常用命令表 write 行改为：

```markdown
| `write --type T --importance N ...` | 写入一条记忆。写入前自动查重：完全重复会跳过（`--allow-duplicate` 强写），同主题新结论会自动建 `supersedes` 链。 |
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 149 passed（关注既有 `test_supersede_demotes_target`：其两条内容 "old approach alpha"/"new approach beta" 主题 Jaccard 0.2 < 0.5，不会被误判 supersede，应保持通过）

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/cli.py README.md tests/test_review_fixes.py
git commit -m "feat(cli): write runs dedup/supersede classification even with --force"
```

---

### Task 5: 发布 0.5.0

**Files:**
- Modify: `mnemosyne/__init__.py`、`CHANGELOG.md`、`README.md`

- [ ] **Step 1: 版本与变更记录**

`__version__` 改为 `"0.5.0"`。`CHANGELOG.md` 顶部新增：

```markdown
## [0.5.0] - 2026-07-04

### 新增 (Added)

- **蒸馏增量化**：Stop hook 按 transcript 记录已处理轮次（store 根 `.distill_state.json`，7 天 TTL），每次只蒸馏新增轮次；transcript 轮换自动从头处理。此前每轮回复结束都全量重蒸馏整个会话。
- **Finding 溯源（evidence）**：`Finding` 新增可选 `evidence` 字段；host 引擎的 `**新发现:**` 块支持 `- evidence:` 行；LLM 引擎要求模型给出对话原文短引用；持久化到记忆 frontmatter 的 `evidence` 键。
- **会话摘要类型（session_summary）**：新记忆类型；`[distill].session_summary = true` 且 `engine = "llm"` 时，每次蒸馏额外产出一条会话级摘要。

### 变更 (Changed)

- **write 统一查重**：`write` 在所有路径（含 `--force`）先做 duplicate/supersede 判定：完全重复跳过写入并提示既有 ID（新 flag `--allow-duplicate` 强写）；同主题新结论自动写入并建 `supersedes` 链、降级旧记忆。"先 search 再写" 从约定变成机制。
```

README「更新日志」段同步一句话。

- [ ] **Step 2: 全量回归 + eval 基线**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 149 passed

Run: `python3 -m mnemosyne eval run`
Expected: bm25-only recall@5=1.000 无回退

- [ ] **Step 3: 提交并合并**

```bash
git add mnemosyne/__init__.py CHANGELOG.md README.md
git commit -m "chore: release 0.5.0"
git checkout master && git merge --no-ff <branch> -m "Merge: 0.5.0 distill upgrades"
```

---

## 后续（0.6，届时单独出计划）

事实有效期（`valid_from`/`invalidated_by`，检索默认过滤失效事实）、maintain 可选 LLM consolidation（sleep-time 整合）、LongMemEval 作为 CI 回归门槛。
