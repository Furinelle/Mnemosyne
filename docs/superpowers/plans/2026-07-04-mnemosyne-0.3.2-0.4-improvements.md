# Mnemosyne 0.3.2 / 0.4.0 改进实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 2026-07-04 代码审查发现的 4 个 P0 正确性 bug（发布 0.3.2），随后完成检索/注入质量的 6 项升级（发布 0.4.0）。

**Architecture:** 全部改动落在现有模块内，不新增子系统：hooks 节流逻辑（session_start）、融合排序（fusion）、frontmatter 解析（schema/store）、生命周期（lifecycle）、注入格式化与会话状态（hooks/_common）、索引查找与增量嵌入（index）、CLI 维护路径（cli）。P2（蒸馏升级）与 P3（时间维度/整合）超出本计划范围，见文末"后续计划"。

**Tech Stack:** Python 3.11+，stdlib + portalocker（唯一硬依赖），SQLite FTS5，pytest（经 uv 运行）。

## Global Constraints

- Python 3.11+；核心路径只允许 stdlib + `portalocker>=2.8`，不得新增硬依赖。
- 测试命令固定为：`uv run --with pytest --with portalocker -- python -m pytest -q`，当前基线 122 passed，每个任务结束时必须全绿。
- 记忆文件格式（frontmatter 字段集）保持向后兼容，不得要求用户迁移已有 `.mnemosyne/` 数据。
- hooks 永不阻塞 Claude：所有 hook 入口保持 `hook_safe()` 包裹，异常吞掉并退出 0。
- 引号风格随文件：`mnemosyne/hooks/*.py` 用单引号，其余核心模块用双引号；新代码匹配所在文件。
- 提交信息遵循仓库现有风格：`fix(scope): ...` / `feat(scope): ...` / `docs: ...` / `chore: ...`。

---

## 阶段 A —— 0.3.2 bug 修复

### Task 1: maintain 节流按 store 分离（修复全局库衰减放大）

**背景**：`maybe_run_maintain` 的 `.last_maintain` 标记写在**项目**库根目录，但触发的是 `maintain --scope all`（含全局库）。同一天在 N 个项目里开会话，全局库被 decay N 次，全局记忆半衰期随活跃项目数失控。

**Files:**
- Modify: `mnemosyne/hooks/session_start.py:63-92`
- Test: `tests/test_hooks.py`（追加 pytest 风格函数）

**Interfaces:**
- Consumes: `find_project_store()`, `global_store()`（`mnemosyne.store`，已存在）
- Produces: `session_start._maintain_due(root: Path) -> bool`、`session_start._mark_maintained(root: Path) -> None`、`session_start._spawn_maintain(scope: str) -> bool`；`maybe_run_maintain()` 签名不变

- [ ] **Step 1: 写失败测试**

在 `tests/test_hooks.py` 末尾追加（该文件已混用 pytest 风格函数，保持一致）：

```python
def test_maybe_run_maintain_throttles_global_across_projects(tmp_path, monkeypatch):
    from mnemosyne.hooks import session_start

    home = tmp_path / "home"
    home.mkdir()
    monkeypatch.setenv("MNEMOSYNE_HOME", str(home))
    calls: list[list[str]] = []
    monkeypatch.setattr(
        session_start.subprocess, "Popen", lambda cmd, **kwargs: calls.append(cmd)
    )

    for name in ("project_a", "project_b"):
        project = tmp_path / name
        (project / ".mnemosyne" / "working").mkdir(parents=True)
        (project / ".mnemosyne" / "core.md").write_text("core", encoding="utf-8")
        monkeypatch.chdir(project)
        session_start.maybe_run_maintain()

    global_calls = [cmd for cmd in calls if "global" in cmd]
    project_calls = [cmd for cmd in calls if "project" in cmd]
    assert len(global_calls) == 1, "global store must be maintained once, not once per project"
    assert len(project_calls) == 2
    assert (home / ".last_maintain").exists()


def test_maybe_run_maintain_runs_again_after_interval(tmp_path, monkeypatch):
    from datetime import datetime, timedelta

    from mnemosyne.hooks import session_start

    home = tmp_path / "home"
    home.mkdir()
    stale = (datetime.now() - timedelta(hours=25)).isoformat()
    (home / ".last_maintain").write_text(stale, encoding="utf-8")
    monkeypatch.setenv("MNEMOSYNE_HOME", str(home))
    monkeypatch.chdir(tmp_path)  # no project store here
    calls: list[list[str]] = []
    monkeypatch.setattr(
        session_start.subprocess, "Popen", lambda cmd, **kwargs: calls.append(cmd)
    )

    session_start.maybe_run_maintain()

    assert [cmd for cmd in calls if "global" in cmd], "stale marker must trigger a new run"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q -k maybe_run_maintain`
Expected: FAIL（第一条：`global_calls` 为 2 不是 1；第二条：无项目库时旧代码直接 return，`calls` 为空）

- [ ] **Step 3: 实现**

将 `mnemosyne/hooks/session_start.py` 中 `maybe_run_maintain` 整体替换为：

```python
def _maintain_due(root: Path) -> bool:
    marker = root / '.last_maintain'
    if not marker.exists():
        return True
    try:
        last = datetime.fromisoformat(marker.read_text(encoding='utf-8').strip())
    except (ValueError, OSError):
        return True
    now = datetime.now(tz=last.tzinfo) if last.tzinfo else datetime.now()
    return now - last >= MAINTAIN_INTERVAL


def _mark_maintained(root: Path) -> None:
    try:
        (root / '.last_maintain').write_text(datetime.now().isoformat(), encoding='utf-8')
    except OSError:
        pass


def _spawn_maintain(scope: str) -> bool:
    kwargs: dict = {'stdout': subprocess.DEVNULL, 'stderr': subprocess.DEVNULL}
    if sys.platform == 'win32':
        kwargs['creationflags'] = subprocess.DETACHED_PROCESS | subprocess.CREATE_NEW_PROCESS_GROUP
    else:
        kwargs['start_new_session'] = True
    try:
        subprocess.Popen(
            [sys.executable, '-m', 'mnemosyne', 'maintain', '--scope', scope],
            **kwargs,
        )
    except OSError:
        return False
    return True


def maybe_run_maintain() -> None:
    """Run background maintenance, throttled per store.

    The throttle marker lives in each store's own root: the global store
    decays once per interval no matter how many projects open sessions that
    day. A project-level marker gating `--scope all` used to multiply global
    decay by the number of active projects.
    """
    project = find_project_store()
    if project is not None and _maintain_due(project.root):
        if _spawn_maintain('project'):
            _mark_maintained(project.root)
    global_root = global_store().root
    if global_root.exists() and _maintain_due(global_root):
        if _spawn_maintain('global'):
            _mark_maintained(global_root)
```

注意：`datetime`、`timedelta`、`subprocess`、`sys`、`Path`、`find_project_store`、`global_store` 均已在该文件 import，无需新增。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q`
Expected: PASS（含既有用例）

- [ ] **Step 5: 全量回归 + 提交**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 124 passed

```bash
git add mnemosyne/hooks/session_start.py tests/test_hooks.py
git commit -m "fix(hooks): throttle maintain per store so global decay stops scaling with active projects"
```

---

### Task 2: rerank 分数刻度修复

**背景**：`_rerank` 只把 top `2N` 条候选的 `score` 覆写为 reranker 分值（通常 0~1），尾部候选保留 BM25/RRF 原始分（BM25-only 时可达 5+），随后全体混排——未参与重排的尾部会反超被 reranker 认可的头部。

**Files:**
- Modify: `mnemosyne/fusion.py:219-231`
- Test: `tests/test_fusion.py`

**Interfaces:**
- Consumes: `FusionSearchResult`、`_sorted_results`（fusion 模块内部，已存在）
- Produces: `_rerank(query, results, reranker, top_n) -> list[FusionSearchResult]` 签名不变；新契约：重排区整体排在未重排尾部之前

- [ ] **Step 1: 写失败测试**

在 `tests/test_fusion.py` 的 `FusionTests` 类中追加（`_memory` 与 `PreferSecondReranker` 已在该文件定义；需在文件头 import 中补 `Store`）：

```python
    def test_rerank_keeps_unreranked_tail_behind_reranked_head(self) -> None:
        from mnemosyne.fusion import _rerank
        from mnemosyne.store import Store

        store = Store("project", Path("."))
        results = [
            FusionSearchResult(store=store, path=Path("a.md"), memory=_memory("first", "first doc"), score=6.0),
            FusionSearchResult(store=store, path=Path("b.md"), memory=_memory("second", "second doc"), score=5.0),
            FusionSearchResult(store=store, path=Path("c.md"), memory=_memory("third", "third doc"), score=4.0),
        ]

        reranked = _rerank("query", results, PreferSecondReranker(), top_n=1)

        self.assertEqual(["second", "first", "third"], [item.memory.id for item in reranked])
```

`top_n=1` 使 `count=2`：前两条被重排（0.1 / 0.9），第三条是分数 4.0 的未重排尾部。旧代码混排后顺序为 `["third", "second", "first"]`。

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_fusion.py -q -k tail_behind`
Expected: FAIL，实际顺序 `['third', 'second', 'first']`

- [ ] **Step 3: 实现**

把 `mnemosyne/fusion.py` 中 `_rerank` 的最后一行 `return _sorted_results(results)` 改为：

```python
    # Reranked candidates always precede the un-reranked tail: reranker scores
    # and BM25/RRF scores live on different scales, so a global sort would let
    # a raw-score tail entry outrank a reranked head entry.
    return _sorted_results(selected) + results[count:]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_fusion.py -q`
Expected: PASS

- [ ] **Step 5: 全量回归 + 提交**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 125 passed

```bash
git add mnemosyne/fusion.py tests/test_fusion.py
git commit -m "fix(fusion): keep un-reranked tail behind reranked head instead of mixing score scales"
```

---

### Task 3: frontmatter 解析容错 + doctor 报告坏文件

**背景**：`parse_memory` 里 `int(known.get("strength"))` 遇到手改坏的值（如 `strength: high`）抛 `ValueError`；`load_memories` 只捕 `OSError`，一个坏文件让 search / maintain / 所有 hook 全部失效（hook 被 `hook_safe` 静默吞掉，更难察觉）。"文件即接口"必须容忍手工编辑。

**Files:**
- Modify: `mnemosyne/schema.py:76-96`
- Modify: `mnemosyne/store.py:273-280`
- Modify: `mnemosyne/cli.py:410-420`（cmd_doctor 的 per-store 检查段）
- Test: `tests/test_review_fixes.py`、`tests/test_doctor.py`

**Interfaces:**
- Produces: `schema._to_int(value, default=0) -> int`；`store.corrupt_memory_paths(store, include_archive=True) -> list[Path]`（doctor 用）
- 契约变化：`parse_memory` 对非数字 `strength`/`access_count` 回退 0 而不抛异常；`load_memories` 跳过无法解码/解析的文件

- [ ] **Step 1: 写失败测试**

`tests/test_review_fixes.py` 追加（该文件为 unittest 风格，沿用 `isolated_workspace`）：

```python
class CorruptFileToleranceTests(unittest.TestCase):
    def test_parse_memory_tolerates_non_numeric_counters(self) -> None:
        from mnemosyne.schema import parse_memory

        text = "---\nid: bad-1\ntype: pitfall\nstrength: high\naccess_count: many\n---\n\n## t\n\nbody\n"

        memory = parse_memory(text)

        self.assertEqual(0, memory.strength)
        self.assertEqual(0, memory.access_count)
        self.assertEqual("bad-1", memory.id)

    def test_load_memories_skips_undecodable_file(self) -> None:
        from mnemosyne.schema import Memory
        from mnemosyne.store import ensure_store, load_memories, project_store, working_path, write_memory

        with isolated_workspace():
            store = project_store()
            ensure_store(store)
            good = Memory(id="good-1", type="codebase", strength=70, body="## ok\n\nfine")
            write_memory(working_path(store, good), good)
            (store.working_dir / "junk.md").write_bytes(b"\xff\xfe\x00 not utf8")

            memories = load_memories(store)

            self.assertEqual(["good-1"], [memory.id for _, memory in memories])

    def test_corrupt_memory_paths_reports_bad_files(self) -> None:
        from mnemosyne.store import corrupt_memory_paths, ensure_store, project_store

        with isolated_workspace():
            store = project_store()
            ensure_store(store)
            (store.working_dir / "junk.md").write_bytes(b"\xff\xfe\x00 not utf8")
            (store.working_dir / "no_id.md").write_text("---\ntype: pitfall\n---\n\nbody\n", encoding="utf-8")

            bad = corrupt_memory_paths(store)

            self.assertEqual({"junk.md", "no_id.md"}, {path.name for path in bad})
```

`tests/test_doctor.py` 追加（沿用该文件既有风格；若为 pytest 函数风格则如下）：

```python
def test_doctor_reports_corrupt_files(tmp_path, monkeypatch, capsys):
    from mnemosyne.cli import main
    from mnemosyne.store import ensure_store, project_store

    monkeypatch.setenv("MNEMOSYNE_HOME", str(tmp_path / "home"))
    project = tmp_path / "project"
    project.mkdir()
    monkeypatch.chdir(project)
    store = project_store()
    ensure_store(store)
    (store.working_dir / "junk.md").write_bytes(b"\xff\xfe not utf8")

    main(["doctor", "--scope", "project"])

    out = capsys.readouterr().out
    assert "corrupt files" in out
    assert "junk.md" in out
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py tests/test_doctor.py -q`
Expected: FAIL（`parse_memory` 抛 ValueError；`load_memories` 抛 UnicodeDecodeError；`corrupt_memory_paths` ImportError；doctor 输出无 corrupt 行）

- [ ] **Step 3: 实现**

`mnemosyne/schema.py`：在 `parse_memory` 上方加：

```python
def _to_int(value: Any, default: int = 0) -> int:
    """Frontmatter is hand-editable markdown; a mangled counter must not make
    the whole memory (and with it every search/maintain/hook) unreadable."""
    try:
        return int(value or 0)
    except (TypeError, ValueError):
        return default
```

`parse_memory` 中两处替换：

```python
        strength=_to_int(known.get("strength")),
        ...
        access_count=_to_int(known.get("access_count")),
```

`mnemosyne/store.py`：`load_memories` 的 `except OSError:` 改为 `except (OSError, ValueError):`（`UnicodeDecodeError` 是 `ValueError` 子类），并新增：

```python
def corrupt_memory_paths(store: Store, include_archive: bool = True) -> list[Path]:
    """Memory files that cannot be used: unreadable, undecodable, or missing an id."""
    bad: list[Path] = []
    for path in iter_memory_paths(store, include_archive=include_archive):
        try:
            memory = parse_memory(path.read_text(encoding="utf-8"))
        except (OSError, ValueError):
            bad.append(path)
            continue
        if not memory.id:
            bad.append(path)
    return bad
```

`mnemosyne/cli.py`：`cmd_doctor` 中 `if initialized:` 分支末尾（`index` 检查之后）追加：

```python
            bad = corrupt_memory_paths(store)
            if bad:
                names = ", ".join(path.name for path in bad[:3])
                more = f" (+{len(bad) - 3} more)" if len(bad) > 3 else ""
                checks.append((f"{store.scope} corrupt files", False, f"{names}{more}; fix or delete, they are skipped", False))
            else:
                checks.append((f"{store.scope} corrupt files", True, "none", False))
```

并在 cli.py 的 store import 列表中加入 `corrupt_memory_paths`。

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py tests/test_doctor.py -q`
Expected: PASS

- [ ] **Step 5: 全量回归 + 提交**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 129 passed

```bash
git add mnemosyne/schema.py mnemosyne/store.py mnemosyne/cli.py tests/test_review_fixes.py tests/test_doctor.py
git commit -m "fix(schema,store): tolerate hand-edited corrupt frontmatter; doctor reports bad files"
```

---

### Task 4: expires 只对 ISO 日期生效，自由文本仅作注记

**背景**：README 示例写的是自由文本（`expires: 认证方案重构时失效`），而 `lifecycle.maintain_memory` 对 `expires` 做 ISO 日期字符串比较。自由文本不被误归档靠的是字典序运气。收紧为：仅 `YYYY-MM-DD` 格式参与到期归档；其余值保留为人类可读注记；`write --expires` 传非日期时打警告。

**Files:**
- Modify: `mnemosyne/lifecycle.py:52-60`
- Modify: `mnemosyne/cli.py`（cmd_write，`path = working_path(...)` 之前）
- Modify: `README.md`（文件格式一节，`expires` 说明）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `lifecycle.is_date_expiry(value: str) -> bool`（cli 复用）

- [ ] **Step 1: 写失败测试**

`tests/test_review_fixes.py` 追加：

```python
class ExpiresSemanticsTests(unittest.TestCase):
    def _memory(self, expires: str):
        from mnemosyne.schema import Memory

        return Memory(id="m-1", type="pitfall", strength=80, expires=expires, body="## t\n\nbody")

    def test_free_text_expires_never_archives(self) -> None:
        from pathlib import Path

        from mnemosyne.lifecycle import maintain_memory
        from mnemosyne.store import Store

        result, _ = maintain_memory(
            Store("project", Path(".")), Path("m-1.md"), self._memory("认证方案重构时失效"),
            {"decay_per_run": 1, "archive_strength": 30, "deprecated_strength": 5,
             "core_strength": 200, "core_access_count": 99},
            dry_run=True,
        )

        self.assertEqual("decayed", result)

    def test_iso_expires_archives_past_date(self) -> None:
        from pathlib import Path

        from mnemosyne.lifecycle import maintain_memory
        from mnemosyne.store import Store

        result, _ = maintain_memory(
            Store("project", Path(".")), Path("m-1.md"), self._memory("2020-01-01"),
            {"decay_per_run": 1, "archive_strength": 30, "deprecated_strength": 5,
             "core_strength": 200, "core_access_count": 99},
            dry_run=True,
        )

        self.assertEqual("archived", result)

    def test_is_date_expiry(self) -> None:
        from mnemosyne.lifecycle import is_date_expiry

        self.assertTrue(is_date_expiry("2026-12-31"))
        self.assertFalse(is_date_expiry("认证方案重构时失效"))
        self.assertFalse(is_date_expiry("2026-1-1"))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py -q -k Expires`
Expected: FAIL（`is_date_expiry` 不存在；自由文本用例当前恰好 pass，保留它防回归）

- [ ] **Step 3: 实现**

`mnemosyne/lifecycle.py` 头部加 `import re`，模块级加：

```python
_ISO_DATE_RE = re.compile(r"^\d{4}-\d{2}-\d{2}$")


def is_date_expiry(value: str) -> bool:
    """Only ISO dates take part in expiry archival; anything else is a
    human-readable note (e.g. '认证方案重构时失效') and must never compare
    lexically against today's date."""
    return bool(_ISO_DATE_RE.match(value.strip()))
```

`maintain_memory` 中的到期判断改为：

```python
    if memory.expires and is_date_expiry(memory.expires) and memory.expires.strip() < date.today().isoformat():
```

`mnemosyne/cli.py` 的 `cmd_write`，在构造 `memory = Memory(...)` 之后加：

```python
    if args.expires and not is_date_expiry(args.expires):
        print(
            "Warning: --expires is not an ISO date (YYYY-MM-DD); kept as a note, will not auto-archive.",
            file=sys.stderr,
        )
```

并在 cli.py 顶部 `from mnemosyne.lifecycle import MaintainSummary, maintain_memory` 中加入 `is_date_expiry`。

`README.md` 文件格式一节，`expires` 行下补一句：

```markdown
`expires` 支持两种写法：ISO 日期（`2026-12-31`，maintain 到期自动归档）或自由文本
条件注记（如 `认证方案重构时失效`，仅供人和 agent 阅读，不参与自动归档）。
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py -q`
Expected: PASS

- [ ] **Step 5: 全量回归 + 提交**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 132 passed

```bash
git add mnemosyne/lifecycle.py mnemosyne/cli.py README.md tests/test_review_fixes.py
git commit -m "fix(lifecycle): only ISO dates trigger expiry archival; free-text expires is a note"
```

---

### Task 5: 发布 0.3.2

**Files:**
- Modify: `mnemosyne/__init__.py`（`__version__`）
- Modify: `CHANGELOG.md`
- Modify: `README.md`（更新日志段落）

- [ ] **Step 1: 版本与变更记录**

`mnemosyne/__init__.py` 中 `__version__` 改为 `"0.3.2"`。`CHANGELOG.md` 顶部新增：

```markdown
## 0.3.2

修复：
- SessionStart 的 maintain 节流标记改为按 store 存放；此前 N 个活跃项目会让全局库每天衰减 N 次。
- rerank 启用时，未参与重排的尾部候选不再与 reranker 分数混排反超头部。
- 手工编辑损坏的 frontmatter（非数字 strength 等）不再拖垮整库读取；`doctor` 新增坏文件报告。
- `expires` 仅在 ISO 日期格式时参与到期归档，自由文本仅作注记；`write --expires` 对非日期值告警。
```

README.md「更新日志」段落同步一句话（沿用现有格式）。

- [ ] **Step 2: 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 132 passed

- [ ] **Step 3: 提交**

```bash
git add mnemosyne/__init__.py CHANGELOG.md README.md
git commit -m "chore: release 0.3.2"
```

---

## 阶段 B —— 0.4.0 检索与注入质量

### Task 6: 注入排序改为相关性优先

**背景**：`format_for_injection` 按 `(strength, score)` 排序，strength 主导——强但不相关的记忆挤掉弱但相关的。改为 `(score, strength)`：检索分主导，strength 只做平分决胜。

**Files:**
- Modify: `mnemosyne/hooks/_common.py:203-207`
- Test: `tests/test_hooks.py`（`InjectionFormattingTests` 类内）

**Interfaces:**
- `format_for_injection(results, max_tokens=None)` 签名不变，仅排序契约变化

- [ ] **Step 1: 写失败测试**

`tests/test_hooks.py` 的 `InjectionFormattingTests` 中追加：

```python
    def test_format_for_injection_ranks_relevance_over_strength(self) -> None:
        results = [
            {
                "id": "strong-stale", "scope": "global", "type": "preference",
                "tags": [], "summary": "irrelevant but strong", "score": 0.5, "strength": 95,
            },
            {
                "id": "weak-relevant", "scope": "project", "type": "pitfall",
                "tags": [], "summary": "exact topical hit", "score": 8.0, "strength": 35,
            },
        ]

        text = format_for_injection(results)

        self.assertLess(text.index("weak-relevant"), text.index("strong-stale"))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q -k relevance_over_strength`
Expected: FAIL（当前 strong-stale 排在前面）

- [ ] **Step 3: 实现**

`_common.py` 中 `format_for_injection` 的排序键改为：

```python
    sorted_results = sorted(
        results,
        key=lambda item: (float(item.get('score', 0) or 0), int(item.get('strength', 0) or 0)),
        reverse=True,
    )
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 133 passed（既有预算截断用例中 memory-1 的 score 与 strength 同序，不受影响）

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/hooks/_common.py tests/test_hooks.py
git commit -m "fix(hooks): rank injected memories by retrieval score, strength as tiebreaker"
```

---

### Task 7: 会话级注入去重

**背景**：UserPromptSubmit / PreToolUse 每次触发都注入 top-N，同一记忆在同一会话内反复注入，浪费上下文预算。Claude Code hook 事件带 `session_id`；用它在 store 根目录维护一个小状态文件，同一会话内已注入的记忆不再重复注入。

**Files:**
- Modify: `mnemosyne/hooks/_common.py`（新增 3 个函数与 import）
- Modify: `mnemosyne/hooks/user_prompt_submit.py`
- Modify: `mnemosyne/hooks/pre_tool_use.py`
- Test: `tests/test_hooks.py`

**Interfaces:**
- Produces: `_common.load_injected_ids(session_id: str) -> set[str]`、`_common.record_injected_ids(session_id: str, memory_ids: list[str]) -> None`
- 状态文件：`<store.root>/.session_injected.json`，结构 `{"sessions": {"<session_id>": {"ts": "<iso>", "ids": [...]}}}`，48 小时 TTL 自动清理

- [ ] **Step 1: 写失败测试**

`tests/test_hooks.py` 追加：

```python
def test_injected_ids_roundtrip(tmp_path, monkeypatch):
    from mnemosyne.hooks import _common

    home = tmp_path / "home"
    home.mkdir()
    monkeypatch.setenv("MNEMOSYNE_HOME", str(home))
    monkeypatch.chdir(tmp_path)  # no project store -> state lives in global root

    _common.record_injected_ids("s1", ["m-1", "m-2"])
    _common.record_injected_ids("s1", ["m-3"])

    assert _common.load_injected_ids("s1") == {"m-1", "m-2", "m-3"}
    assert _common.load_injected_ids("s2") == set()
    assert _common.load_injected_ids("") == set()


def test_user_prompt_submit_skips_already_injected(tmp_path, monkeypatch, capsys):
    import io
    import json as jsonlib

    from mnemosyne.hooks import user_prompt_submit
    from mnemosyne.schema import Memory
    from mnemosyne.store import ensure_store, project_store, working_path, write_memory

    monkeypatch.setenv("MNEMOSYNE_HOME", str(tmp_path / "home"))
    project = tmp_path / "project"
    project.mkdir()
    monkeypatch.chdir(project)
    store = project_store()
    ensure_store(store)
    memory = Memory(
        id="pitfall-1", type="pitfall", strength=80,
        injection_summary="JWT 认证失败排查记录",
        body="## JWT 认证失败排查\n\ntoken 过期导致认证失败",
        tags=["jwt"],
    )
    write_memory(working_path(store, memory), memory)

    event = jsonlib.dumps({"session_id": "s1", "prompt": "JWT 认证失败怎么排查一下"})

    monkeypatch.setattr("sys.stdin", io.StringIO(event))
    user_prompt_submit.main()
    first = capsys.readouterr().out
    assert "pitfall-1" in first

    monkeypatch.setattr("sys.stdin", io.StringIO(event))
    user_prompt_submit.main()
    second = capsys.readouterr().out
    assert "pitfall-1" not in second
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q -k injected`
Expected: FAIL（`record_injected_ids` 不存在；第二次注入仍包含 pitfall-1）

- [ ] **Step 3: 实现 _common.py**

文件头部 import 区加入 `import json`、`import os`，并把 `from datetime import date` 改为 `from datetime import date, datetime, timedelta`。`read_event` 内部的 `import json` 可同时删除。模块级追加：

```python
SESSION_STATE_FILENAME = '.session_injected.json'
SESSION_STATE_TTL_HOURS = 48


def _session_state_path() -> Path:
    project = find_project_store()
    root = project.root if project is not None else global_store().root
    return root / SESSION_STATE_FILENAME


def load_injected_ids(session_id: str) -> set[str]:
    if not session_id:
        return set()
    try:
        data = json.loads(_session_state_path().read_text(encoding='utf-8'))
    except (OSError, ValueError):
        return set()
    entry = data.get('sessions', {}).get(session_id, {})
    return set(entry.get('ids', []))


def record_injected_ids(session_id: str, memory_ids: list[str]) -> None:
    """Remember which memories this session has already seen.

    Injection hooks fire on every prompt and edit; without this, the same
    memory is re-injected each turn and quietly eats the context budget.
    Sessions older than the TTL are pruned so the state file stays small.
    """
    if not session_id or not memory_ids:
        return
    path = _session_state_path()
    try:
        data = json.loads(path.read_text(encoding='utf-8'))
    except (OSError, ValueError):
        data = {}
    sessions = data.setdefault('sessions', {})
    now = datetime.now()
    cutoff = now - timedelta(hours=SESSION_STATE_TTL_HOURS)
    for key in list(sessions):
        try:
            stamp = datetime.fromisoformat(str(sessions[key].get('ts', '')))
        except ValueError:
            stamp = None
        if stamp is None or stamp < cutoff:
            del sessions[key]
    entry = sessions.setdefault(session_id, {'ts': now.isoformat(), 'ids': []})
    entry['ts'] = now.isoformat()
    entry['ids'] = sorted(set(entry['ids']) | set(memory_ids))
    try:
        path.parent.mkdir(parents=True, exist_ok=True)
        tmp = path.with_suffix('.tmp')
        tmp.write_text(json.dumps(data, ensure_ascii=False), encoding='utf-8')
        os.replace(tmp, path)
    except OSError:
        pass
```

- [ ] **Step 4: 实现两个 hook 入口**

`user_prompt_submit.py` 的 `main` 改为：

```python
def main() -> None:
    with hook_safe():
        event = read_event()
        prompt = (event.get('prompt') or '').strip()
        if len(prompt) < 10:
            return
        keywords = extract_keywords(prompt, limit=8)
        if not keywords:
            return
        session_id = str(event.get('session_id') or '')
        already = load_injected_ids(session_id)
        results = run_search(' '.join(keywords), limit=3, update_access=True)
        fresh = [item for item in results if item['id'] not in already]
        if not fresh:
            return
        stores = collect_stores()
        config = load_config(stores[-1] if stores else None)
        max_tokens = int(config.get('injection', {}).get('max_tokens', 2000))
        context = format_for_injection(fresh, max_tokens=max_tokens)
        if not context:
            return
        record_injected_ids(session_id, [item['id'] for item in fresh])
        output = {
            'hookSpecificOutput': {
                'hookEventName': 'UserPromptSubmit',
                'additionalContext': context,
            }
        }
        print(json.dumps(output, ensure_ascii=False))
```

import 行改为 `from mnemosyne.hooks._common import (collect_stores, extract_keywords, format_for_injection, hook_safe, load_injected_ids, read_event, record_injected_ids, run_search)`。

`pre_tool_use.py` 同样处理：`run_search(basename, limit=2, update_access=False)` 结果先按 `already` 过滤，注入后 `record_injected_ids(session_id, ...)`，import 相应补齐。核心差异部分：

```python
        session_id = str(event.get('session_id') or '')
        already = load_injected_ids(session_id)
        results = [item for item in run_search(basename, limit=2, update_access=False) if item['id'] not in already]
        if not results:
            return
        ...
        record_injected_ids(session_id, [item['id'] for item in results])
```

（`record_injected_ids` 放在拼好 `context` 确认非空之后、`print` 之前。）

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 135 passed

- [ ] **Step 6: 提交**

```bash
git add mnemosyne/hooks/_common.py mnemosyne/hooks/user_prompt_submit.py mnemosyne/hooks/pre_tool_use.py tests/test_hooks.py
git commit -m "feat(hooks): session-level injection dedup via .session_injected.json state"
```

---

### Task 8: link expansion 经 SQLite 查找目标记忆

**背景**：`expand_links` 对每个候选的每条链接调 `find_memory`，后者线性重读整库文件，检索成本 O(候选×链接×N)。改为优先查 `memories_meta` 表（memory_id → path），查不到再回退线性扫描；同一次扩展内做 memoize。

**Files:**
- Modify: `mnemosyne/index.py`（新增函数）
- Modify: `mnemosyne/fusion.py:93-139`
- Test: `tests/test_index_v2.py`（新增函数的单测；expand_links 行为不变由既有 fusion 测试保障）

**Interfaces:**
- Produces: `index.lookup_indexed_memory(stores: Iterable[Store], memory_id: str) -> tuple[Store, Path, Memory] | None`

- [ ] **Step 1: 写失败测试**

`tests/test_index_v2.py` 追加（沿用该文件既有风格；若为 unittest 类则放入新类）：

```python
class LookupIndexedMemoryTests(unittest.TestCase):
    def test_lookup_via_index_and_fallback(self) -> None:
        from mnemosyne.index import lookup_indexed_memory, reindex_store
        from mnemosyne.schema import Memory
        from mnemosyne.store import ensure_store, project_store, working_path, write_memory

        with isolated_workspace():
            store = project_store()
            ensure_store(store)
            memory = Memory(id="target-1", type="codebase", strength=70, body="## t\n\nbody")
            write_memory(working_path(store, memory), memory)
            reindex_store(store)

            found = lookup_indexed_memory([store], "target-1")
            self.assertIsNotNone(found)
            self.assertEqual("target-1", found[2].id)

            self.assertIsNone(lookup_indexed_memory([store], "missing-1"))
```

（若 `tests/test_index_v2.py` 未 import `isolated_workspace` / `unittest`，在文件头补上，与仓库其余测试一致。）

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_index_v2.py -q -k lookup`
Expected: FAIL with ImportError

- [ ] **Step 3: 实现 index.py**

`mnemosyne/index.py` 中 `delete_memory_index` 之后追加：

```python
def lookup_indexed_memory(
    stores: Iterable[Store], memory_id: str
) -> tuple[Store, Path, Memory] | None:
    """Resolve a memory id via the persistent index instead of scanning files.

    Link expansion resolves every linked id of every candidate; a linear
    re-read of the whole store per link makes search O(candidates x links x N).
    """
    for store in stores:
        if not index_path(store).exists():
            continue
        try:
            with closing(_connect(index_path(store))) as connection:
                row = connection.execute(
                    "SELECT path FROM memories_meta WHERE memory_id = ? LIMIT 1",
                    (memory_id,),
                ).fetchone()
        except sqlite3.Error:
            continue
        if row is None:
            continue
        path = Path(row[0])
        try:
            return store, path, parse_memory(path.read_text(encoding="utf-8"))
        except (OSError, ValueError):
            continue
    return None
```

- [ ] **Step 4: 实现 fusion.py**

`expand_links` 中，`for hop in range(max_hops):` 之前加：

```python
    resolved: dict[str, tuple[Store, Path, Memory] | None] = {}

    def resolve(target_id: str) -> tuple[Store, Path, Memory] | None:
        if target_id not in resolved:
            found = lookup_indexed_memory(stores, target_id)
            if found is None:
                found = find_memory(target_id, stores, include_archive=True)
            resolved[target_id] = found
        return resolved[target_id]
```

并把循环体内的 `found = find_memory(target_id, stores, include_archive=True)` 改为 `found = resolve(target_id)`。fusion.py 的 index import 行加入 `lookup_indexed_memory`。

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 136 passed（既有 `test_link_expansion_uses_typed_relation_weight` 等用例验证行为未变）

- [ ] **Step 6: 提交**

```bash
git add mnemosyne/index.py mnemosyne/fusion.py tests/test_index_v2.py
git commit -m "perf(fusion): resolve link-expansion targets via SQLite meta with per-call memoization"
```

---

### Task 9: embedding 增量刷新 + 分批 + 失败告警

**背景**：`backfill_embeddings` 每次全量重嵌所有记忆，且整批共用一个 30s 超时、失败时 `fallback=[]` 静默返回——库大时一条都写不进且无任何提示；记忆编辑后旧向量也永不刷新。改为：只嵌"缺失 / 换模型 / 内容已更新（`embedding_mtime < mtime`）"的记忆，按 `embedding.batch_size` 分批，每批独立超时，失败批打 stderr 警告。

**Files:**
- Modify: `mnemosyne/index.py:460-474`
- Test: `tests/test_embedding.py`

**Interfaces:**
- `backfill_embeddings(store, embedder, include_archive=True, batch_size=None) -> int` —— 新增可选 `batch_size`（None 时读 `[embedding].batch_size`，默认 32）；返回值仍为本次写入的向量数
- 契约变化：第二次调用（无变更时）返回 0

- [ ] **Step 1: 写失败测试**

`tests/test_embedding.py` 追加：

```python
class IncrementalBackfillTests(unittest.TestCase):
    class CountingEmbedder:
        model_id = "hash-v1"
        dimensions = 2

        def __init__(self) -> None:
            self.calls: list[int] = []

        def embed(self, texts: list[str]) -> list[list[float]]:
            self.calls.append(len(texts))
            return [[1.0, 0.0] for _ in texts]

        def embed_one(self, text: str) -> list[float]:
            return [1.0, 0.0]

    def test_backfill_only_embeds_stale_memories(self) -> None:
        import os
        import time

        from mnemosyne.index import backfill_embeddings, reindex_store
        from mnemosyne.schema import Memory
        from mnemosyne.store import ensure_store, project_store, working_path, write_memory

        with isolated_workspace():
            store = project_store()
            ensure_store(store)
            paths = {}
            for name in ("alpha", "beta"):
                memory = Memory(id=name, type="codebase", strength=70, body=f"## {name}\n\n{name} body")
                path = working_path(store, memory)
                write_memory(path, memory)
                paths[name] = path
            reindex_store(store)
            embedder = self.CountingEmbedder()

            self.assertEqual(2, backfill_embeddings(store, embedder))
            self.assertEqual(0, backfill_embeddings(store, embedder), "fresh embeddings must be skipped")

            bumped = time.time() + 5
            os.utime(paths["alpha"], (bumped, bumped))
            self.assertEqual(1, backfill_embeddings(store, embedder), "edited memory must be re-embedded")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_embedding.py -q -k stale`
Expected: FAIL（第二次调用返回 2 而非 0）

- [ ] **Step 3: 实现**

`mnemosyne/index.py` 头部加 `import sys`，`backfill_embeddings` 整体替换为：

```python
def backfill_embeddings(
    store: Store,
    embedder,
    include_archive: bool = True,
    batch_size: int | None = None,
) -> int:
    """Embed memories whose vector is missing, from another model, or stale.

    Batching gives every chunk its own timeout: one slow call skips that
    chunk with a warning instead of silently writing zero embeddings for the
    whole store, which is what a single whole-run timeout used to do.
    """
    sync_index(store, include_archive=include_archive)
    if batch_size is None:
        batch_size = int(load_config(store).get("embedding", {}).get("batch_size", 32))
    batch_size = max(1, batch_size)
    filters = ["(embedding IS NULL OR embedding_model != ? OR embedding_mtime < mtime)"]
    params: list[object] = [embedder.model_id]
    if not include_archive:
        filters.append("archived = 0")
    with closing(_connect(index_path(store))) as connection:
        connection.row_factory = sqlite3.Row
        rows = connection.execute(
            f"SELECT memory_id, path FROM memories_meta WHERE {' AND '.join(filters)}",
            params,
        ).fetchall()
    count = 0
    for start in range(0, len(rows), batch_size):
        chunk = rows[start : start + batch_size]
        ids: list[str] = []
        texts: list[str] = []
        for row in chunk:
            try:
                memory = parse_memory(Path(row["path"]).read_text(encoding="utf-8"))
            except (OSError, ValueError):
                continue
            ids.append(row["memory_id"])
            texts.append(memory_search_text(memory))
        if not texts:
            continue
        vectors = call_with_timeout(lambda: embedder.embed(texts), timeout=30.0, fallback=None)
        if vectors is None or len(vectors) != len(ids):
            print(
                f"mnemosyne: embedding batch failed; skipped {len(ids)} memories in {store.scope} store",
                file=sys.stderr,
            )
            continue
        for memory_id, vector in zip(ids, vectors):
            if vector:
                write_embedding(store, memory_id, vector, embedder.model_id)
                count += 1
    return count
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 137 passed。若 `tests/test_embedding.py` 中有既有用例断言"第二次 backfill 重嵌全部"，把断言更新为 0（增量是新契约）并在提交信息中注明。

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/index.py tests/test_embedding.py
git commit -m "feat(index): incremental embedding backfill with per-batch timeout and stderr warnings"
```

---

### Task 10: 注入瘦身（progressive disclosure）

**背景**：当前每条注入占两行、摘要 220 字。参考 claude-mem 的分层披露：注入只给单行「id + 短摘要」，末尾附一行提示 agent 用 `show` 拉全文。摘要长度经 `[injection].summary_chars` 可配（默认 120）。

**Files:**
- Modify: `mnemosyne/hooks/_common.py`（`format_for_injection`）
- Modify: `mnemosyne/store.py`（`DEFAULT_CONFIG` 与 `DEFAULT_CONFIG_TOML` 的 `[injection]` 段）
- Modify: `mnemosyne/hooks/user_prompt_submit.py`、`mnemosyne/hooks/pre_tool_use.py`、`mnemosyne/codex.py`（调用处传参）
- Modify: `README.md`（配置说明）
- Test: `tests/test_hooks.py`

**Interfaces:**
- `format_for_injection(results, max_tokens=None, summary_chars=120) -> str` —— 新增可选参数；输出契约：每条一行 `- (scope/type) id [tags]: summary`，非空输出末尾含 `mnemosyne show` 提示行

- [ ] **Step 1: 写失败测试**

`tests/test_hooks.py` 的 `InjectionFormattingTests` 中追加：

```python
    def test_format_for_injection_single_line_with_show_hint(self) -> None:
        results = [
            {
                "id": "memory-1", "scope": "project", "type": "codebase",
                "tags": ["alpha"], "summary": "A" * 300, "score": 3.0, "strength": 90,
            },
        ]

        text = format_for_injection(results)

        entry_lines = [line for line in text.splitlines() if line.startswith("- ")]
        self.assertEqual(1, len(entry_lines))
        self.assertLess(len(entry_lines[0]), 200)
        self.assertIn("memory-1", entry_lines[0])
        self.assertIn("mnemosyne show", text)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_hooks.py -q -k show_hint`
Expected: FAIL（当前是两行格式且无 footer）

- [ ] **Step 3: 实现 format_for_injection**

`_common.py` 中整体替换（排序键保持 Task 6 的相关性优先）：

```python
def format_for_injection(
    results: list[dict],
    max_tokens: int | None = None,
    summary_chars: int = 120,
) -> str:
    """One line per memory plus a `show` hint: the injection is a table of
    contents, not the content. Agents pull full bodies on demand, which keeps
    the per-prompt context cost near-constant as the store grows."""
    if not results:
        return ''
    lines: list[str] = ['## Relevant memories from Mnemosyne', '']
    footer = 'Run `python3 -m mnemosyne show <id>` for full detail.'
    used_tokens = _approx_tokens('\n'.join(lines)) + _approx_tokens(footer)
    sorted_results = sorted(
        results,
        key=lambda item: (float(item.get('score', 0) or 0), int(item.get('strength', 0) or 0)),
        reverse=True,
    )
    emitted = 0
    for item in sorted_results:
        tag_part = f" [{', '.join(item['tags'])}]" if item['tags'] else ''
        summary = ' '.join(str(item['summary']).split())
        if len(summary) > summary_chars:
            summary = summary[: max(1, summary_chars - 3)].rstrip() + '...'
        line = f"- ({item['scope']}/{item['type']}) {item['id']}{tag_part}: {summary}"
        line_tokens = _approx_tokens(line)
        if max_tokens is not None and used_tokens + line_tokens > max_tokens:
            if emitted == 0:
                remaining = max(0, (max_tokens - used_tokens - 8) * 4)
                if remaining > 0:
                    lines.append(line[:remaining].rstrip() + '...')
                    emitted += 1
            break
        lines.append(line)
        used_tokens += line_tokens
        emitted += 1
    if emitted == 0:
        return ''
    lines.extend(['', footer])
    return '\n'.join(lines)
```

- [ ] **Step 4: 配置与调用处**

`mnemosyne/store.py`：`DEFAULT_CONFIG["injection"]` 改为 `{"max_tokens": 2000, "summary_chars": 120}`；`DEFAULT_CONFIG_TOML` 的 `[injection]` 段同步加 `summary_chars = 120`。

三个调用处（`user_prompt_submit.py`、`pre_tool_use.py`、`codex.py` 的 `prep`）在读取 `max_tokens` 处同步读取并传入：

```python
        summary_chars = int(config.get('injection', {}).get('summary_chars', 120))
        context = format_for_injection(fresh, max_tokens=max_tokens, summary_chars=summary_chars)
```

（`pre_tool_use` 用 `results`，`codex.prep` 用其局部变量名，其余同型。）

`README.md` 配置说明一节 `[injection]` 示例加 `summary_chars = 120`，并在说明列表补一句："`injection.summary_chars` 控制注入摘要的单条截断长度；完整内容用 `show` 获取。"

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 138 passed。既有预算截断用例（`test_format_for_injection_respects_approx_token_budget`）在单行格式下仍应成立：memory-1 被截为 120 字符入选，memory-2 超预算被丢弃；若边界字符数因 footer 计入而变化导致断言失败，将该用例的 `max_tokens=80` 调到 `max_tokens=90` 并保留两条断言语义。

- [ ] **Step 6: 提交**

```bash
git add mnemosyne/hooks/_common.py mnemosyne/store.py mnemosyne/hooks/user_prompt_submit.py mnemosyne/hooks/pre_tool_use.py mnemosyne/codex.py README.md tests/test_hooks.py
git commit -m "feat(hooks): single-line injection entries with show hint (progressive disclosure)"
```

---

### Task 11: maintain 时全量重写 MEMORY.md

**背景**：`update_memory_index_file` 只追加；归档/取代的条目永远留在 MEMORY.md 里，越长越误导。写入路径保持廉价追加，maintain（非 dry-run）时全量重建对账。

**Files:**
- Modify: `mnemosyne/cli.py`（新增 `rewrite_memory_index_file`；`cmd_maintain` 调用）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `cli.rewrite_memory_index_file(store: Store) -> None`

- [ ] **Step 1: 写失败测试**

`tests/test_review_fixes.py` 追加：

```python
class MemoryIndexRewriteTests(unittest.TestCase):
    def test_maintain_rewrites_memory_index(self) -> None:
        from mnemosyne.cli import main, update_memory_index_file
        from mnemosyne.schema import Memory
        from mnemosyne.store import ensure_store, project_store, working_path, write_memory

        with isolated_workspace():
            store = project_store()
            ensure_store(store)
            keep = Memory(id="keep-1", type="codebase", strength=90,
                          injection_summary="stays", body="## keep\n\nstays")
            drop = Memory(id="drop-1", type="pitfall", strength=25,
                          injection_summary="archived away", body="## drop\n\ngoes")
            for memory in (keep, drop):
                write_memory(working_path(store, memory), memory)
                update_memory_index_file(store, memory)

            main(["maintain", "--scope", "project"])

            content = (store.root / "MEMORY.md").read_text(encoding="utf-8")
            self.assertIn("keep-1", content)
            self.assertNotIn("drop-1", content, "archived memories must leave MEMORY.md")
```

（strength 25 < archive_strength 30，maintain 时被归档。）

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run --with pytest --with portalocker -- python -m pytest tests/test_review_fixes.py -q -k rewrites_memory_index`
Expected: FAIL（drop-1 仍在 MEMORY.md）

- [ ] **Step 3: 实现**

`mnemosyne/cli.py` 中 `update_memory_index_file` 之后追加：

```python
def rewrite_memory_index_file(store: Store) -> None:
    """Regenerate MEMORY.md from active working memories.

    The write path appends for cheapness; maintain reconciles so entries for
    archived or superseded memories do not accumulate forever.
    """
    try:
        header = template_text("MEMORY.md").rstrip()
    except (FileNotFoundError, OSError):
        header = "# Memory Index"
    lines = [header, ""]
    memories = sorted(load_memories(store), key=lambda pair: pair[1].strength, reverse=True)
    for _path, memory in memories:
        lines.append(f"- `{memory.id}` ({memory.type}, strength {memory.strength}): {memory.injection_summary}")
    (store.root / "MEMORY.md").write_text("\n".join(lines) + "\n", encoding="utf-8")
```

`cmd_maintain` 中，`if index_enabled(store) and index_path(store).exists():` 之前加：

```python
        if not args.dry_run:
            rewrite_memory_index_file(store)
```

- [ ] **Step 4: 跑测试确认通过 + 全量回归**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 139 passed

- [ ] **Step 5: 提交**

```bash
git add mnemosyne/cli.py tests/test_review_fixes.py
git commit -m "fix(cli): maintain rewrites MEMORY.md so archived entries stop accumulating"
```

---

### Task 12: 发布 0.4.0

**Files:**
- Modify: `mnemosyne/__init__.py`、`CHANGELOG.md`、`README.md`

- [ ] **Step 1: 版本与变更记录**

`__version__` 改为 `"0.4.0"`。`CHANGELOG.md` 顶部新增：

```markdown
## 0.4.0

检索与注入质量：
- 注入排序改为检索相关性优先，strength 仅作平分决胜。
- 会话级注入去重：同一会话内已注入的记忆不再重复注入（store 根目录 `.session_injected.json`，48h TTL）。
- 注入改为单行摘要 + `show` 提示（progressive disclosure），新增 `injection.summary_chars` 配置（默认 120）。
- link expansion 改经 SQLite meta 表解析链接目标，并在单次检索内 memoize。
- embedding backfill 增量化：只嵌缺失/换模型/内容更新的记忆，分批独立超时，失败批 stderr 告警。
- maintain 全量重写 MEMORY.md，归档条目不再残留。
```

README「更新日志」段同步；「排障」表加一行：「同一会话记忆没有再次注入 → 属会话级去重预期行为，需要全文用 `show ID`」。

- [ ] **Step 2: 全量回归 + LongMemEval 快速基线**

Run: `uv run --with pytest --with portalocker -- python -m pytest -q`
Expected: 139 passed

Run: `python3 -m mnemosyne eval run 2>/dev/null || true`（有内置 50 条语料基线则记录 recall/MRR 于 commit message；不可用则跳过并注明）

- [ ] **Step 3: 提交**

```bash
git add mnemosyne/__init__.py CHANGELOG.md README.md
git commit -m "chore: release 0.4.0"
```

---

## 后续计划（不在本计划内，届时单独出计划文档）

**0.5 —— 蒸馏升级（P2）**，建议单独计划，范围：
- distill 增量化：在 store 根记录每个 transcript 的已处理 offset/turn 数，Stop hook 只蒸馏新增轮次（当前每轮全量重蒸馏）。
- LLM 引擎一等公民：typed observations（bugfix/discovery/decision）、新增 `session_summary` 记忆类型、蒸馏结果携带 transcript 行号溯源。
- `write` 路径统一接入 `classify_against_store`，把"先 search 再写"从 CLAUDE.md 约定变成机制（`--force` 仅跳过交互确认，不跳过 dedup 判定）。

**0.6 —— 时间维度与整合（P3）**，建议单独计划，范围：
- 事实有效期（对标 Graphiti）：frontmatter 增加 `valid_from` / `invalidated_by`，supersede 时自动填充，检索默认过滤已失效事实。
- Sleep-time 整合（对标 Letta）：maintain 挂可选 LLM consolidation——合并近重复记忆、生成 core.md 晋升草稿、压缩老化 working 记忆；复用现有 24h 节流与 opt-in 配置模式。
- LongMemEval 作为检索管线改动的回归门槛（CI 化）。

**已知未排期的小问题（backlog）**：归档搬移与搜索回写的竞态窗口（低概率复活已归档记忆）；tokenizer 不认假名/谚文；PreToolUse 用 basename 检索命中过泛；搜索命中即写盘的写放大与 strength 饱和（rich-get-richer，需要与衰减模型一起重新设计）。
