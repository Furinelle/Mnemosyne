# Mnemosyne 0.6.0 时间维度与整合实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 路线图 P3 三项：被取代事实默认退出检索（对标 Graphiti 的失效不删除）、`consolidate` 近重复整合命令（对标 Letta sleep-time 的保守 v1，纯启发式）、eval 回归门槛 + GitHub Actions CI。

**Architecture:** supersede 时旧记忆标记 `status = "superseded"` 并记录 `invalidated_by`；`fusion.search` 输出层单点过滤（所有 lane 统一生效），`search --include-superseded` 可回看历史。`consolidate` 是新 CLI 子命令，默认 dry-run，`--commit` 时用既有 `merge_memory` 合并强弱记忆并删除弱者。`eval run --min-recall` 提供退出码门槛，CI 用它守住检索质量。

**Tech Stack:** Python 3.11+，stdlib + portalocker，GitHub Actions。

## Global Constraints

- 测试命令：`uv run --with pytest --with portalocker -- python -m pytest -q`，基线 149 passed，每任务结束全绿。
- 失效不删除：superseded 记忆文件保留原位，只改 status/extra；`--include-superseded` 必须能完整召回。
- `consolidate` 默认 dry-run，任何删除/合并只在 `--commit` 下发生。
- 其余约束同 0.5 计划（stdlib-only、hooks 不阻塞、引号风格随文件、commit 风格）。

---

### Task 1: superseded 记忆默认退出检索

**Files:**
- Modify: `mnemosyne/distill/__init__.py`（`_apply_supersedes`）
- Modify: `mnemosyne/cli.py`（`cmd_link` 的 demoting 分支；search 子命令加 flag）
- Modify: `mnemosyne/fusion.py`（`search` 加 `include_superseded` 参数与输出层过滤）
- Modify: `README.md`
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `fusion.search(..., include_superseded: bool = False)`；`search --include-superseded` CLI flag
- 契约：被 supersede 的记忆 `status == "superseded"`、`extra["invalidated_by"] == <new_id>`；默认检索不返回，flag 打开时返回

- [ ] **Step 1: 失败测试**（`tests/test_review_fixes.py` 追加）

```python
class SupersededRetrievalTests(unittest.TestCase):
    def _write_pair(self):
        self.assertEqual(0, main(["init"]))
        self.assertEqual(0, main([
            "write", "--type", "arch_decision", "--importance", "60", "--force",
            "--title", "JWT storage decision", "--content", "use localStorage for JWT",
        ]))
        store = project_store()
        old_id = load_memories(store)[0][1].id
        self.assertEqual(0, main([
            "write", "--type", "arch_decision", "--importance", "60", "--force",
            "--title", "JWT storage decision",
            "--content", "switch to httpOnly cookie for JWT storage",
        ]))
        new_id = next(m.id for _, m in load_memories(store) if m.id != old_id)
        return store, old_id, new_id

    def test_superseded_memory_is_marked_and_filtered(self) -> None:
        from mnemosyne.fusion import search as fusion_search

        with isolated_workspace():
            store, old_id, new_id = self._write_pair()
            memories = {m.id: m for _, m in load_memories(store)}
            self.assertEqual("superseded", memories[old_id].status)
            self.assertEqual(new_id, memories[old_id].extra.get("invalidated_by"))

            default_ids = [r.memory.id for r in fusion_search([store], "JWT storage", limit=5)]
            self.assertNotIn(old_id, default_ids)
            self.assertIn(new_id, default_ids)

            all_ids = [r.memory.id for r in fusion_search([store], "JWT storage", limit=5, include_superseded=True)]
            self.assertIn(old_id, all_ids)

    def test_cli_search_include_superseded_flag(self) -> None:
        import io
        from contextlib import redirect_stdout

        with isolated_workspace():
            store, old_id, _new_id = self._write_pair()
            output = io.StringIO()
            with redirect_stdout(output):
                main(["search", "JWT storage", "--format", "json", "--include-superseded"])
            self.assertIn(old_id, output.getvalue())
```

- [ ] **Step 2: 确认失败** — Run: `... -k Superseded`，Expected: FAIL（status 仍 active；flag 不存在）

- [ ] **Step 3: 实现**

`distill/__init__.py::_apply_supersedes`，`old_memory.strength = ...` 行后加：

```python
    old_memory.status = "superseded"
    old_memory.extra["invalidated_by"] = new_id
```

`cli.py::cmd_link` 的 `if is_demoting(args.rel):` 分支同样加：

```python
        second_memory.status = "superseded"
        second_memory.extra["invalidated_by"] = first_memory.id
```

`fusion.py::search` 签名加 `include_superseded: bool = False`；`return results[:limit]` 前加：

```python
    if not include_superseded:
        # Invalidation, not deletion: superseded facts stay on disk and are
        # recallable with include_superseded, but must not win default recall.
        results = [r for r in results if r.memory.status != "superseded"]
```

（注意过滤须在 rerank 之后、截断 limit 之前。）

`cli.py`：search 子命令加 `search_parser.add_argument("--include-superseded", action="store_true", help="include memories superseded by newer ones")`；`cmd_search` 调 `fusion_search(..., include_superseded=args.include_superseded)`。

`README.md`：搜索索引一节补一句「被 `supersedes` 取代的记忆默认不出现在搜索结果中（失效不删除），用 `search --include-superseded` 回看历史结论；旧记忆 frontmatter 带 `invalidated_by` 指向取代者。」

- [ ] **Step 4: 全量回归**（Expected: 151 passed）→ **Step 5: 提交** `feat(fusion): superseded memories drop out of default retrieval (invalidate, don't delete)`

---

### Task 2: consolidate 近重复整合命令

**Files:**
- Modify: `mnemosyne/cli.py`（新子命令 + `cmd_consolidate` + `_consolidation_pairs`）
- Modify: `README.md`（常用命令表）
- Test: `tests/test_review_fixes.py`

**Interfaces:**
- Produces: `mnemosyne consolidate [--scope global|project|all] [--threshold 0.8] [--commit]`
- 契约：同 type、`jaccard(tokenize(summary+body)) >= threshold` 的记忆对被识别；dry-run 只打印；`--commit` 将弱者并入强者（`merge_memory`）、删除弱者文件与索引行、重写 MEMORY.md

- [ ] **Step 1: 失败测试**

```python
class ConsolidateTests(unittest.TestCase):
    def _seed(self):
        self.assertEqual(0, main(["init"]))
        for importance, content in (("80", "use httpOnly cookie for JWT tokens"),
                                     ("60", "use httpOnly cookie for JWT auth tokens")):
            self.assertEqual(0, main([
                "write", "--type", "pitfall", "--importance", importance, "--force",
                "--allow-duplicate", "--title", f"JWT cookie {importance}",
                "--content", content,
            ]))

    def test_consolidate_dry_run_lists_pairs_without_changes(self) -> None:
        import io
        from contextlib import redirect_stdout

        with isolated_workspace():
            self._seed()
            output = io.StringIO()

            with redirect_stdout(output):
                code = main(["consolidate", "--scope", "project"])

            self.assertEqual(0, code)
            self.assertIn("would merge", output.getvalue())
            self.assertEqual(2, len(load_memories(project_store())))

    def test_consolidate_commit_merges_weaker_into_stronger(self) -> None:
        with isolated_workspace():
            self._seed()

            self.assertEqual(0, main(["consolidate", "--scope", "project", "--commit"]))

            memories = load_memories(project_store())
            self.assertEqual(1, len(memories))
            survivor = memories[0][1]
            self.assertEqual(80, survivor.strength)
            self.assertIn("auth tokens", survivor.body)
```

- [ ] **Step 2: 确认失败** — Run: `... -k Consolidate`，Expected: FAIL（unknown command）

- [ ] **Step 3: 实现**（`cli.py`）

`build_parser` 中 `maintain` 之后加：

```python
    consolidate_parser = subparsers.add_parser(
        "consolidate", help="Merge near-duplicate working memories (dry-run by default)")
    consolidate_parser.add_argument("--scope", choices=["global", "project", "all"], default="project")
    consolidate_parser.add_argument("--threshold", type=float, default=0.8,
        help="Jaccard similarity above which two same-type memories merge")
    consolidate_parser.add_argument("--commit", action="store_true",
        help="actually merge (default: preview only)")
    consolidate_parser.set_defaults(func=cmd_consolidate)
```

实现（放在 `cmd_maintain` 之后）：

```python
def _consolidation_tokens(memory: Memory) -> set[str]:
    from mnemosyne.search import tokenize as _tokenize

    return set(_tokenize(f"{memory.canonical_summary} {memory.body}"))


def cmd_consolidate(args: argparse.Namespace) -> int:
    from mnemosyne.distill import jaccard  # same similarity the dedup path uses
    from mnemosyne.index import remove_memory_index

    merged_total = 0
    for store in stores_for_scope(args.scope):
        with lock_store(store):
            entries = load_memories(store)
            entries.sort(key=lambda pair: pair[1].strength, reverse=True)
            consumed: set[str] = set()
            for i, (strong_path, strong) in enumerate(entries):
                if strong.id in consumed:
                    continue
                strong_tokens = _consolidation_tokens(strong)
                for weak_path, weak in entries[i + 1:]:
                    if weak.id in consumed or weak.type != strong.type:
                        continue
                    similarity = jaccard(sorted(strong_tokens), sorted(_consolidation_tokens(weak)))
                    if similarity < args.threshold:
                        continue
                    if not args.commit:
                        print(f"would merge {weak.id} -> {strong.id} (similarity {similarity:.2f})")
                        consumed.add(weak.id)
                        continue
                    merge_memory(strong, weak)
                    write_memory(strong_path, strong)
                    update_search_index(store, strong_path, strong)
                    weak_path.unlink(missing_ok=True)
                    remove_memory_index(store, weak.id)
                    consumed.add(weak.id)
                    merged_total += 1
                    print(f"merged {weak.id} -> {strong.id} (similarity {similarity:.2f})")
            if args.commit and consumed:
                rewrite_memory_index_file(store)
    if not args.commit:
        print("(dry-run) Add --commit to merge.")
    else:
        print(f"merged: {merged_total}")
    return 0
```

（`jaccard` 接受 list；传 `sorted(set)`。`merge_memory`、`rewrite_memory_index_file`、`remove_memory_index` 均为既有函数。）

`README.md` 常用命令表加：`| consolidate [--commit] | 合并近重复 working 记忆（同类型、相似度 ≥0.8）；默认 dry-run 预览。 |`

- [ ] **Step 4: 全量回归**（Expected: 153 passed）→ **Step 5: 提交** `feat(cli): consolidate command merges near-duplicate memories (dry-run by default)`

---

### Task 3: eval 回归门槛 + GitHub Actions CI

**Files:**
- Modify: `mnemosyne/eval/__main__.py`
- Create: `.github/workflows/ci.yml`
- Test: `tests/test_eval.py`

**Interfaces:**
- Produces: `eval run --min-recall X` —— 非 longmemeval 路径结束时 `recall@5 < X` 则打印 FAIL 并返回 1

- [ ] **Step 1: 失败测试**（`tests/test_eval.py` 追加，沿用该文件风格）

```python
def test_eval_run_min_recall_gate():
    from mnemosyne.eval.__main__ import main as eval_main

    assert eval_main(["run", "--min-recall", "1.01"]) == 1
    assert eval_main(["run", "--min-recall", "0.5"]) == 0
```

- [ ] **Step 2: 确认失败** — unknown argument `--min-recall`

- [ ] **Step 3: 实现**

`run_parser` 加：

```python
    run_parser.add_argument("--min-recall", type=float, default=None,
        help="Exit non-zero if bm25 recall@5 falls below this threshold (CI regression gate)")
```

`cmd_run` 非 longmemeval 路径 `return 0` 前加：

```python
    if args.min_recall is not None and current["recall@5"] < args.min_recall:
        print(f"FAIL: recall@5 {current['recall@5']:.3f} < required {args.min_recall:.3f}")
        return 1
```

`.github/workflows/ci.yml`：

```yaml
name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -e . pytest
      - run: python -m pytest -q
      - run: python -m mnemosyne eval run --min-recall 0.95
```

- [ ] **Step 4: 全量回归**（Expected: 154 passed）→ **Step 5: 提交** `feat(eval): --min-recall CI gate + GitHub Actions workflow`

---

### Task 4: 发布 0.6.0 + 合并

- [ ] `__version__ = "0.6.0"`；CHANGELOG 新增 0.6.0 段（三项：superseded 检索过滤 + invalidated_by、consolidate 命令、eval 门槛与 CI）；README 更新日志段同步。
- [ ] 全量回归 + `python3 -m mnemosyne eval run --min-recall 0.95`（验证门槛本身通过）。
- [ ] `chore: release 0.6.0`，合并 master（`Merge: 0.6.0 temporal validity + consolidation + CI gate`），删除分支，master 上复跑测试。
